# WYSIWYG 全链路修复实施计划

最终状态（2026-09-07）：F1–F8 全部完成，专项复审与全分支复审的发现均已关闭。开发分支 `codex/wysiwyg-chain-repair`；后续交付要求为提交并推送远端 `main`。

验证：`./scripts/verify.sh` 最终退出码为 0，架构边界、cargo fmt、Clippy（warnings as errors）、工作区单元/集成/文档测试全部通过。测试汇总 4812 passed、0 failed、5 ignored；其中 Markdown 1299 单元测试、18 项新链路回归、textora-app 927 单元测试通过。同步 mock server 在允许本地端口的环境验证，27 项通过。最终日志 `/tmp/textora-full-verify-final.log`。

验证范围：使用真实 parser、编辑事务、插件 render/query、shaping 和应用事件链；未进行真实系统输入法人工操作，也未对超大文档组合输入做专项性能基准。

> 按已授权的全量修复逐项实施；每项先复现再修复，最终独立复审和完整验证。

目标：修复同日 full-chain-audit 中 F1–F8，并验证相关 Unicode、选区和语法边界。

架构：源码仍是持久化事实源。明确真实内容、折叠语法、合成文字的投影；编辑增强只消费对应语义节点；导航查询准备当前源码布局；搜索使用源码匹配范围映射几何；IME 使用临时替换投影。所有 UI 输入保持纯数据，不引入 app 状态依赖。

约束：中文沟通；每项最多先安排三个相关文件，跨模块追加修改独立拆分；Rust 命名自解释，避免魔法值和 unwrap，cargo fmt；不改旧测试预期掩盖缺陷；不替换用户已有改动。暂不提交，最后运行 ./scripts/verify.sh。

## 任务与验收

- [x] A1：builder.rs、projection.rs、独立投影回归测试：修复图片 F1 及实体等合成内容，验证全文合法锚点、点击与导航。
- [x] A2：builder.rs、edit.rs、投影回归测试：修复代码 F2，覆盖中文、多个反引号、规范化空格与换行、完整标记展开。
- [x] B1：augmenter.rs 内新增正反向删除失败测试，修复 F3；代码和转义文本按字素删除，真实 InlineHtml 换行仍整体删除。
- [x] B2：paste/writer.rs 内新增转换再解析保真测试，修复 F4；覆盖 <br>、&copy;、表格和链接文字。
- [x] B3：commands.rs 内新增含反引号选区的解析测试，修复 F5；安全长度及代码首尾空格，toggle 还原、LF/CRLF。
- [x] C：view.rs、应用导航入口及测试：修复 F6；UpdateSource 后、下一次绘制之前导航能使用当前版本投影，不使用旧偏移兜底。
- [x] D：view.rs、搜索几何模块及测试：修复 F7；按源码匹配范围绘制当前/非当前匹配，与 app 搜索序号一致；核实正则、Unicode 与滚动坐标。
- [x] E：edit.rs、view.rs、IME 回归测试：修复 F8；临时显示替换选区，原始文档保持可取消，正反向/跨行与提交保持一致。
- [x] F：应用事务与事件顺序回归，确认撤销/重做恢复源码和光标；独立复审所有改动。
- [x] G：cargo fmt，./scripts/verify.sh 全部通过；在审查文档登记最终覆盖与限制。

每个阶段先运行报告附录或等价正式测试，确认失败原因与审查一致；实施后运行定向测试及相关模块基线。A、B 为独立实现域，B 可交子任务，避免共享文件写入。C、D、E 顺序处理 view.rs。

## 进度

开始版本 550785d；工作区无 Rust 修改，仅同日审查报告。审查基线 Markdown 1282 通过，app 924 单元测试及12集成通过；实现阶段将重新验证。

### 本轮实施记录

- A1/A2 已实现：图片与解码实体采用完整源码区间，代码使用真实分隔符/内容映射；补充 CRLF、容器、表格和相邻代码节点回归。独立复审发现相邻同样式节点误合并造成中文切片崩溃，已限定为同一源码节点的片段合并并通过正式红/绿测试。
- B1/B2/B3 已实现且独立复审：语义换行删除、粘贴字符转义、动态代码分隔符；复审发现较长 closing fence 的合法代码块无法取消格式，已补测试修复。
- C 已实现：UpdateSource 使用最近样式/宽度发布当前 generation 的无 shaping 布局；后续 cursor 更新刷新受影响块；绘制再精确化可见区域。插件与 app 连续输入、不插入绘制的回归通过，undo/redo 的组合测试正在全量验证。
- D 已实现：UI 添加纯数据源码匹配范围查询，appkit-shell 转发 SearchState 的匹配列表及 generation，Markdown 投影成高亮；组合字素部分匹配扩到完整字素。原始高亮和折叠标记、颜色序号测试通过。
- E 已实现：选区替换的独立 IME 预览布局，保留原文/光标/选区；跨块、反选、取消、提交及同文提交、组合字素、候选窗与下划线测试通过。组合期间隐藏原文搜索高亮。
- F/G 进行中：最终跨模块独立审查及 ./scripts/verify.sh。首次验证发现两项新增测试代码的 clippy 布局/单区间 Vec 告警，已修正；第二次已通过架构检查、fmt、clippy，工作区测试进行中。

### 独立复审与验证进展

- A1/A2 复审通过：相邻代码节点的 UTF-8 崩溃及 ASCII 重复问题已按源码节点所有权修复。
- B1–B3 复审通过：较长闭合围栏的取消格式已修复。
- 全分支复审 F1–F7 未发现新增可操作问题；F8 发现组合期间导航改变真实选区后仍复用旧预览选区，已分派补充红/绿测试和修复。
- 完整验证第二轮：架构、格式、Clippy通过；Markdown1296单元+18链路通过；工作区测试在 sync 的12个本地mock服务绑定用例受沙箱限制而退出。已在允许本地端口的环境单独重跑 textora-sync，27/27通过。最终代码完成后将再次完整运行验证脚本。
- 新增 app 搜索范围转发契约测试通过，确认插件不会自行重复搜索或重新编号。所有系统输入法候选窗/光标验证仍为真实render/query API自动化；未进行人工系统输入法操作。

### 最终复审收敛

F8 导航边界已修复并复审通过：预览每次依据当前真实字节选区同步；无选区时退出替换副本，回到当前光标的预编辑；光标/选区变化与相同候选消息都同步；全选直接发布原文范围。新增导航、扩选/收缩和全选三组红/绿回归，Markdown单元测试1299项通过。独立原始反例重跑后，导航后的预览与提交均为 `old 新tail`。

真实 textora 应用事件链 `dispatch_wysiwyg_navigation → dispatch_transactional_edit` 的 IME 回归已通过；插入/undo/redo 后不绘制的方向键回归和权威搜索范围转发回归均通过。最终完整验证正在允许本地mock服务端口的环境运行，完成后登记结果。

修复阶段的完整验证已完成并通过，所有修复任务关闭。交付时同步远端 main，并对整合后的代码重新验证。
